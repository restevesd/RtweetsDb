"description" TEXT,
"statusesCount" REAL,
"followersCount" REAL,
"favoritesCount" REAL,
"friendsCount" REAL,
"url" TEXT,
"name" TEXT,
"created" REAL,
"protected" INTEGER,
"verified" INTEGER,
"screenName" TEXT NOT NULL UNIQUE,
"location" TEXT,
"lang" TEXT,
"id" TEXT PRIMARY KEY,
"listedCount" REAL,
"followRequestSent" INTEGER,
"profileImageUrl" TEXT 
